/** Validates challenger transaction by:
 * - checking that the user has registered
 * - checking that the cart is not empty
 */
const validateChallengerTransaction = (req, res, next) => {
  next();
};
